# add-official-docker-openai-tts-proxy

Add non-invasive Docker deployment for official GPT-SoVITS with CPU default, GPU profile, and a separate OpenAI-compatible TTS proxy that forwards to the official /tts API without modifying upstream source files.
